<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicha.as_android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AlugueService"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaginaPrincipalActivity" />
        <activity android:name=".PaginaPrincipal_PreAluguelActivity" />
        <activity android:name=".Consultar_PreAluguelActivity" />
        <activity android:name=".Consultar_ProdutoActivity" />
        <activity android:name=".Alterar_Status_ProdutoActivity" />
        <activity android:name=".Criar_PreAluguelActivity" />
        <activity android:name=".PaginaPrincipial_ProdutosActivity"></activity>
    </application>

</manifest>