<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nicha.as_android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoas"
        android:label="AlugueService"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PaginaPrincipalActivity" />
        <activity android:name=".pre_aluguel.MenuActivity" />
        <activity android:name=".pre_aluguel.ConsultarActivity" />
        <activity android:name=".pre_aluguel.CriarActivity" />
        <activity android:name=".pre_aluguel.ResultadoActivity" />
        <activity android:name=".produto.ResultadoActivity" />
        <activity android:name=".produto.ConsultarActivity" />
        <activity android:name=".produto.MenuActivity" />
        <activity android:name=".produto.StatusActivity"></activity>
    </application>

</manifest>